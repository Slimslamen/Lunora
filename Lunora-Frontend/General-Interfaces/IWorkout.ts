export interface IWorkout {
    name: string,
    goal: string,
    phase: string,
    duration: number,
    calories: string,
    intensity: string,
    type: string,
    muscles: string[],
    exercises: string[],
    createdAt: string,
    updatedAt: string,
}